node_modules
dist
*.log
yarn-error.log

# 环境变量（账号密码、邮箱授权码）绝不入库
.env
.env.*
!.env.example
src/constant.my.ts

# 运行时状态（sqlite session/token、Playwright profile、FIT 文件、日志）一律放在
# ~/.dailysync/ ，不在仓库里。这两条只是防止误建同名目录时被提交。
db/
data/
logs/
garmin_fit_files/

.DS_Store
.idea
/.vscode
.maestro/
